# 백엔드 환경변수 설정 예시
# 실제 사용 시 .env로 복사하고 값을 설정하세요

# ===========================================
# 데이터베이스 설정
# ===========================================

# 데이터베이스 타입 선택 (mysql 또는 nosql)
DATABASE_TYPE=mysql

# MySQL 설정 (DATABASE_TYPE=mysql 사용 시)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=rhymix

# OCI NoSQL Database 설정 (DATABASE_TYPE=nosql 사용 시, 레거시)
NOSQL_COMPARTMENT_ID=ocid1.compartment.oc1..your-compartment-id
NOSQL_TABLE_NAME=photos

# ===========================================
# 스토리지 설정
# ===========================================

# 스토리지 타입 (LOCAL 또는 OCI)
STORAGE_TYPE=OCI

# OCI Object Storage 설정
OCI_NAMESPACE=your-oci-namespace
OCI_BUCKET_NAME=photolog-storage
OCI_REGION=ap-chuncheon-1

# ===========================================
# 인증 및 보안 설정
# ===========================================

# JWT 비밀키
SECRET_KEY=photolog-secret-key

# 텔레그램 봇 설정 (2FA 인증용)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# 허용된 이메일 (관리자)
ALLOWED_EMAIL=your-email@example.com

# ===========================================
# 서버 설정
# ===========================================

# 서버 포트 및 호스트
SERVER_PORT=8001
SERVER_HOST=0.0.0.0

# 파일 업로드 제한 (MB)
MAX_FILE_SIZE=50

# 로그 레벨
LOG_LEVEL=INFO